/**
 * Host canónico del sitio público: apex `lawzora.com` (sin www). Fuente única de verdad para
 * metadataBase, canonical, Open Graph, sitemap y robots. El `www` redirige aquí (regla en Cloudflare).
 */
export const SITE_URL = 'https://lawzora.com';
