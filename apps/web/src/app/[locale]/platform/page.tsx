import { PlatformConsole } from '@/components/lexora/platform-console';

/** Consola del super-admin de plataforma (dueño). Auth propia (no la sesión del despacho). */
export default function PlatformPage() {
  return <PlatformConsole />;
}
